import * as React from 'react';
import ProjectsPage from '../../kit/modules/projects_page';

export default class ProjectsScene extends React.Component<any, any> {
  render() {
    return <ProjectsPage />;
  }
}
