import * as React from 'react';
import LoginPage from '../../kit/modules/login_page'

export default class LoginScene extends React.Component<any, any> {
    render() {
        return <LoginPage />;
    }
}
